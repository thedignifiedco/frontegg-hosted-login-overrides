# Frontegg Overrides Serverless API - Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# Required Configuration
# ============================================

# Default AppId for default customization
# This should match your Frontegg application ID
# You can find this in your Frontegg dashboard under Settings
DEFAULT_APP_ID=your-frontegg-app-id-here

# ============================================
# Optional: Default Customization Assets
# ============================================

# Logo URL for the default customization
# This will be used in the login box
LOGO_URL=https://your-domain.com/pharmacy.png

# Background image URL for the default customization
# This will be used as the login page background
BACKGROUND_IMAGE_URL=https://your-domain.com/molecule-pattern-background.jpg

# ============================================
# Optional: Alternative Customization Assets
# ============================================

# Alternative logo URL (if you create alternative customizations)
# Use this if you have multiple AppIds with different branding
ALT_LOGO_URL=https://your-domain.com/alt-logo.png

# ============================================
# Notes
# ============================================
# 
# 1. Make sure all URLs are publicly accessible (not behind authentication)
# 2. Use HTTPS URLs for production deployments
# 3. For local development, you can use http://localhost:3000 URLs
# 4. After setting these variables, update src/config/appCustomizations.ts
#    to map your AppIds to the appropriate customizations

